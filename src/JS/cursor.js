import curDot from 'cursor-dot'

const cursor = curDot({
    zIndex: 2,
    diameter: 80,
    borderWidth: 1,
    borderColor: 'Orange',
    easing: 10,
    background: 'tranparent',
})
export default cursor
